# =============================================================================
# RAG QA System - 环境变量示例
# =============================================================================
# 可复制为 .env 后按需填写。
# .env 可能包含敏感信息，请勿提交到 Git 仓库。
# =============================================================================

# -----------------------------------------------------------------------------
# 数据库连接（可选）
# -----------------------------------------------------------------------------
# 项目当前支持在 application.yml 里直接配置 Supabase。
# 如果你想改为环境变量方式，可启用下面参数。
# DB_URL=jdbc:postgresql://db.your-project-ref.supabase.co:5432/postgres?sslmode=require
# DB_USERNAME=postgres.your-project-ref
# DB_PASSWORD=your_supabase_password
# DB_SSL_MODE=require

# -----------------------------------------------------------------------------
# ModelScope API 配置（必须）
# -----------------------------------------------------------------------------
# 获取地址：https://www.modelscope.cn/my/myaccesstoken
# 必须修改：ModelScope API Key
MODELSCOPE_API_KEY=your_modelscope_api_key_here

# -----------------------------------------------------------------------------
# AI提供商配置加密密钥（可选但推荐设置）
# -----------------------------------------------------------------------------
# 用于加密存储在数据库中的API密钥
# 长度必须至少32个字符，建议使用随机字符串
# 生成命令：openssl rand -base64 32
APP_ENCRYPTION_KEY=your_32_character_encryption_key_here!!

# -----------------------------------------------------------------------------
# JWT 认证配置（必须）
# -----------------------------------------------------------------------------
# 必须修改：JWT 签名密钥（至少32个字符）
# 生成命令：openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long

# -----------------------------------------------------------------------------
# 阿里云 OCR 配置（可选）
# -----------------------------------------------------------------------------
# 用于扫描PDF和图片的文字识别
# 获取地址：https://www.aliyun.com/product/ocr
ALIYUN_OCR_APPCODE=your_aliyun_ocr_appcode_here

# -----------------------------------------------------------------------------
# 日志级别配置（可选）
# -----------------------------------------------------------------------------
# 可选值：TRACE, DEBUG, INFO, WARN, ERROR
# LOG_LEVEL_ROOT=INFO
# LOG_LEVEL_APP=INFO
